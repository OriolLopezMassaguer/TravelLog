# Copy this file to keystore.properties (git-ignored) and fill in your values.
# Generate a keystore with:
#   keytool -genkey -v -keystore travellog-release.jks -keyalg RSA -keysize 2048 \
#           -validity 10000 -alias travellog
#
storeFile=../travellog-release.jks
storePassword=YOUR_STORE_PASSWORD
keyAlias=travellog
keyPassword=YOUR_KEY_PASSWORD
